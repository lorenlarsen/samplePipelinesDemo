language: none

build:
  ci:
    - echo 'CI Running'
    - export IMAGE_NAME=gcr.io/clever-web/clever-docker
    - ./build.sh

  post_ci:
    - ./push.sh

integrations:
  hub:
    - integrationName: "gcr-integration"
      type: gcr

