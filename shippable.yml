language: none

build:
  ci:
    - echo 'CI Running'
    - export IMAGE_NAME=gcr.io/clever-web-162023
    - ./build.sh

  post_ci:
    - ./push.sh

integrations:
  hub:
    - integrationName: "gcr-integration"
      type: gcr

